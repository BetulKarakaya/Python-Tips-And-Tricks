# This Python script demonstrates the usage of the bin() function.
# The bin() function converts an integer number to a binary string prefixed with "0b".
# It is useful for converting integer numbers into binary representation.

# Convert an integer to a binary string
number = 42
binary_representation = bin(number)

# Print the binary representation
print("Binary representation of", number, "is:", binary_representation)
